import { CustomDerectiveDirective } from './custom-derective.directive';

describe('CustomDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
